import {Canvas, Meta, Props, Story} from '@storybook/addon-docs';

import {FormDecorator, FormioComponent} from './story-util';

<Meta
  title="Form.io components/Number"
  argTypes={{
    id: {
      type: {name: 'string'},
    },
    label: {
      type: {name: 'string', required: false},
    },
  }}
  decorators={[FormDecorator]}
  parameters={{
    controls: {sort: 'requiredFirst'},
    docs: {
      source: {
        type: 'dynamic',
        excludeDecorators: true,
      },
    },
  }}
/>

# Number

## Default

export const Template = ({id = '', label = '', ...args}) => (
  <FormioComponent
    component={{
      key: id,
      type: 'number',
      input: true,
      label,
    }}
  />
);

<Canvas>
  <Story name="Default" args={{id: 'amount', label: 'Aantal'}}>
    {Template.bind({})}
  </Story>
</Canvas>

<Props story="Default" />
