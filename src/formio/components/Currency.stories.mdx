import { Canvas, Meta, Story, Props } from '@storybook/addon-docs';
import { FormioComponent, FormDecorator } from './story-util';

<Meta
  title="Form.io components/Currency"
  argTypes={{
    id: {
      type: { name: 'string' },
    },
    label: {
      type: { name: 'string', required: false },
    },
  }}
  decorators={[FormDecorator]}
  parameters={{
    controls: { sort: 'requiredFirst' }
  }}
/>

# Currency

## Default

export const Template = ({ id = '', label = '', ...args}) => (
  <FormioComponent component={{
      key: id,
      type: 'currency',
      input: true,
      label,
}}/>);

<Canvas>
  <Story name="Default" args={{ id: 'currency-amount', label: 'Bedrag'}}>
    {Template.bind({})}
  </Story>
</Canvas>

<Props story="Default" />
