import {Canvas, Meta, Story, ArgsTable} from '@storybook/addon-docs';
import {FormioComponent, FormDecorator} from './story-util';

<Meta
  title="Form.io components/Signature"
  argTypes={{
    description: {
      type: {name: 'string', required: false},
    },
    label: {
      type: {name: 'string', required: true},
    },
  }}
  decorators={[FormDecorator]}
  parameters={{
    controls: {sort: 'requiredFirst'},
  }}
/>

# Signature

A signature field is a special field that allows someone to sign the field with either their finger
on a touch enabled device or with the mouse pointer. This signature will be converted into an image
and stored with the form submission.


## Default

export const Template = ({description = '', label = '', ...args}) => (
  <FormioComponent
    component={{
      description, input: true, isSensitiveData: true, key: 'id', label, showInEmail: false, showInPDF: true, showInSummary: true, tableView: false, type: 'signature',
    }}
  />
);

<Canvas>
  <Story
    name="Default"
    args={{ description: 'Plaats hierboven uw handtekening', label: 'Handtekening'}}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
