import { Canvas, Meta, Story, Props } from '@storybook/addon-docs';
import { FormioComponent, FormDecorator } from './story-util';

<Meta
  title="Form.io components/LicensePlateField"
  argTypes={{
    id: {
      type: { name: 'string' },
    },
    label: {
      type: { name: 'string', required: false },
    },
  }}
  decorators={[FormDecorator]}
  parameters={{
    controls: { sort: 'requiredFirst' }
  }}
/>

# LicensePlateField

## Default

export const Template = ({ id = '', label = '', ...args}) => (
  <FormioComponent component={{
      key: id,
      type: 'licenseplate',
      input: true,
      label,
}}/>);

<Canvas>
  <Story name="Default" args={{ id: 'license-plate', label: 'Kentekennummer'}}>
    {Template.bind({})}
  </Story>
</Canvas>

<Props story="Default" />
