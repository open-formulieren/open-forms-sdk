import {Meta} from '@storybook/blocks';

import * as SelectboxesStories from './Selectboxes.stories';

<Meta of={SelectboxesStories} />

# Select Boxes

The Select Boxes allows the user to check multiple values from a list of options.

Options are objects with a `value` and `label` property - the `value` is what is persisted in the
database while the `label` is what's shown to the users.
