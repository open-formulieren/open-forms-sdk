import {Canvas, Meta, Props, Story} from '@storybook/addon-docs';

import LoginButtonIcon from './LoginButtonIcon';

<Meta
  title="Pure React components/Login Button Icon"
  component={LoginButtonIcon}
  argTypes={{
    children: {table: {disable: true}},
    href: {
      control: {
        type: 'text',
      },
    },
    imageSrc: {
      control: {
        type: 'text',
      },
    },
    title: {
      control: {
        type: 'text',
      },
    },
    appearance: {
      control: {
        type: 'text',
      },
    },
  }}
  parameters={{
    controls: {sort: 'requiredFirst'},
  }}
/>

# Login Button

Focus-color depends on whether the Login Button logos have a dark or light colored background.

export const Template = ({
  appearance,
  identifier = '6f64d852-f927-48b8-9b72-dae7fa9d4c18',
  imageSrc,
  href,
  title,
  ...args
}) => <LoginButtonIcon identifier={identifier} logo={{appearance, title, imageSrc, href}} {...args} />;

<Canvas>
  <Story
    name="Dark"
    args={{appearance: 'dark', title: 'Login with DigiD', imageSrc: './digid.png', href: 'https://example.com/login'}}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Light"
    args={{appearance: 'light', title: 'Login with eHerkenning', imageSrc: './eherkenning.png', href: 'https://example.com/login'}}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<Props of={LoginButtonIcon} />
