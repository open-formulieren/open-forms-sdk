import { Canvas, Meta, Story, Props } from '@storybook/addon-docs';

import { Table, TableBody, TableCaption, TableRow, TableHead, TableHeader, TableCell } from "./Table";
import Caption from "./Caption";

<Meta
  title="Pure React components/Table"
  component={Table}
  argTypes={{
    children: {table: {disable: true}},
  }}
  parameters={{
    controls: {sort: 'requiredFirst'}
  }}
/>

# Table

export const Template = () => (
  <Table>
    <TableCaption>Table caption</TableCaption>
    <TableHeader>
      <TableRow>
        <TableHead>Header cell</TableHead>
        <TableHead>Header cell</TableHead>
      </TableRow>
    </TableHeader>
    <TableBody>
      <TableRow>
        <TableCell>Data cell</TableCell>
        <TableCell>Data cell</TableCell>
      </TableRow>
    </TableBody>
  </Table>
);

<Canvas>
  <Story name="Default">
    {Template.bind({})}
  </Story>
</Canvas>

<Props of={Table} />
