import {ArgTypes, Canvas, Meta} from '@storybook/blocks';

import {FormioComponent} from '.';
import * as FormioStories from './index.stories';

<Meta of={FormioStories} />

# Formio renderer

This package implements a mimimal Form.io render in pure React, using Formik.

<div style={{background: '#fff3cd', padding: '2px 10px', marginBottom: '1em'}}>
**DISCLAIMER**

This implementation will be fleshed out and provided in `@open-formulieren/formio-renderer`. Until
that is ready, we provide this implementation.

</div>

## Props

<ArgTypes of={FormioComponent} />
