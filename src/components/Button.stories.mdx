import { Canvas, Meta, Story, Props } from '@storybook/addon-docs';

import Anchor from "./Anchor";
import Button, {VARIANTS} from "./Button";
import FAIcon from "./FAIcon";

<Meta
  title="Pure React components/Button"
  component={Button}
  argTypes={{
    variant: {
      options: VARIANTS,
      control: {
        type: 'radio',
      }
    },
    extraVariants: {
      type: { name: 'array' }
    },
    children: {table: {disable: true}},
    onDisabledClick: {table: {disable: true}},
  }}
  parameters={{
    controls: {sort: 'requiredFirst'},
  }}
/>

# Buttons

Any additional props are passed down to the button component (or custom component, if
using that).

export const Template = ({label='Button', ...args}) => (
  <Button {...args}>{label}</Button>
);

<Canvas>
  <Story name="Extra props" args={{title: 'Title attribute', label: 'Hover me'}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Variants

<Canvas>
  <Story name="Default" args={{label: 'Default'}}>
    {Template.bind({})}
  </Story>
  <Story name="Primary" args={{label: 'Primary', variant: 'primary'}}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary" args={{label: 'Secondary', variant: 'secondary'}}>
    {Template.bind({})}
  </Story>
  <Story name="Danger" args={{label: 'Danger', variant: 'danger'}}>
    {Template.bind({})}
  </Story>
  <Story name="Anchor" args={{label: 'UtrechtButton', variant: 'anchor'}}>
    {Template.bind({})}
  </Story>
  <Story name="Anchor/button" args={{label: 'button tag', variant: 'anchor', component: 'button'}}>
    {Template.bind({})}
  </Story>
  <Story name="Anchor/a" args={{label: 'anchor tag', variant: 'anchor', component: 'a', href: '#'}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Icon buttons

export const IconButtonTemplate = ({icon='pen', ...args}) => (
  <Button {...args}><FAIcon icon={icon} /></Button>
);

<Canvas>
  <Story name="Icon only" args={{title: 'Icon only', variant: 'icon-only'}}>
    {IconButtonTemplate.bind({})}
  </Story>
  <Story name="Icon only danger" args={{title: 'Icon only danger', variant: 'icon-only', extraVariants: ['danger']}}>
    {IconButtonTemplate.bind({})}
  </Story>
</Canvas>

## Disabled state

Passing the `disabled` prop sets the appropriate `aria-disabled` attribute.

<Canvas>
  <Story name="Disabled" args={{
    disabled: true,
    onDisabledClick: () => alert('clicked disabled button'),
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Replacing the HTML element

You can pass an alternative `component` to render instead of the default `a` tag. This
may be either:

* built in HTML tags, passing a string (e.g. `span`)
* custom React components (e.g. `component={MyComponent}`)

<Canvas>
  <Story name="CustomComponent" args={{
    component: 'div',
    value: 'div element',
  }}>
    {Template.bind({})}
  </Story>
  <Story name="Link" args={{label: 'Link', component: Anchor}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<Props of={Button} />
