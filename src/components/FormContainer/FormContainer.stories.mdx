import {Canvas, Meta, Story} from '@storybook/addon-docs';
import {Field} from 'formik';

import {FormikDecorator} from '../../story-utils/decorators';

<Meta
  title="Private API / Appointments / Form Container"
  component={Field}
  parameters={{
    formik: {
      initialValues: {
        test: 'test',
      },
    },
  }}
/>

export const Template = () => <Field type="text" as="input" name="test" />;

<Canvas>
  <Story name="Default" decorators={[FormikDecorator]}>
    {Template.bind({})}
  </Story>
</Canvas>
