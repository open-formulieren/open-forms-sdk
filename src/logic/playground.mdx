import {Meta} from '@storybook/addon-docs/blocks';

import JsonLogicPlayground from './playground';

<Meta title="Playground / JsonLogic" />

# JsonLogic

In the playground you can try out <code>JsonLogic</code> expressions with sample input data. The
official reference documentation is hosted on [jsonlogic.com](https://jsonlogic.com), but you may
find that [JSON Logic engine](https://json-logic.github.io/json-logic-engine/) works better.

<JsonLogicPlayground />
